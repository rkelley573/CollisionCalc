<resources>

    <!-- Splash theme (framework theme) -->
    <style name="Theme.CollisionCalc.Splash" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="android:statusBarColor">@android:color/black</item>
    </style>

    <!-- App theme (still framework, but ensures background isn't weird) -->
    <style name="Theme.CollisionCalc" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
    </style>

</resources>
